import React from 'react';
import { Routes } from '@/router/tools';
import './index.less';

const Content = () => {
    return (
        <div className="m-content">
            <Routes origin />
        </div>
    );
};

export default Content;
